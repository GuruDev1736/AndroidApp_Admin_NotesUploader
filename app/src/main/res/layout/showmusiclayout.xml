<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@drawable/singlrowdesign"
    android:layout_margin="10dp"
    android:padding="10dp"
    android:id="@+id/rlMusic"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/img1"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/music" />

    <TextView
        android:id="@+id/tvMusicName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/img1"
        android:layout_alignBottom="@+id/img1"
        android:layout_marginStart="20dp"
        android:layout_toEndOf="@id/img1"
        android:gravity="center"
        android:text="This is Header Text"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:textStyle="bold" />

    <ImageButton
        android:id="@+id/ibtnPlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/tvMusicName"
        android:layout_alignBottom="@+id/tvMusicName"
        android:layout_marginStart="20dp"
        android:layout_toEndOf="@+id/tvMusicName"
        android:background="@color/black"
        android:clickable="true"
        android:focusable="true"
        android:src="@drawable/baseline_play_arrow_24" />


</RelativeLayout>